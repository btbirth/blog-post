<%if current_user == @post.user%>
	<h1>Edit</h1>
	<%= simple_form_for @post do |f| %>
	  <%= f.input :title %>
	  <%= f.input :body %>
	  <%= f.input :image %>
	  <%= f.button :submit %>
	<% end %>

<%else%>
	<p>you do not have permission to edit this blog</p>
<%end%>